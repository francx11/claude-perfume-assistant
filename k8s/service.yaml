apiVersion: v1
kind: Service
metadata:
  name: perfumeshop-service
spec:
  selector:
    app: perfumeshop      
  ports:
    - port: 80             
      targetPort: 8000     
  type: ClusterIP          
